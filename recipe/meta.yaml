{% set name = "diot" %}
{% set version = "0.1.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/diot-{{ version }}.tar.gz
  sha256: 1dcfc37dff406c96020d14350c05f920cb7446c3e5abf3477cf2ae0587f72066

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - poetry >=0.12
    - python >=3.6
  run:
    - inflection <1.0.0
    - python >=3.6

test:
  requires:
    - pip
  imports:
    - diot
  commands:
    - python -m pip check

about:
  home: https://github.com/pwwang/diot
  dev_url: https://github.com/pwwang/diot.git
  doc_url: https://pwwang.github.io/diot/
  summary: Python dictionary with dot notation
  license: MIT
  license_file: LICENSE
  description: |
    Python dictionary with dot notation (A re-implementation of
    python-box with some issues fixed and simplified)

extra:
  recipe-maintainers:
    - duncanmmacleod
